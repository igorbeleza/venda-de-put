# Arquivo local de checkouts antigos

Pastas abaixo **não vão para o git**.

## `main-antes-do-reset/`

Zip da árvore Go que morava em `C:\dev\venda-de-put` (`master`) no momento em que `main` passou a ser este dashboard Python.

Zip: `main-antes-do-reset/venda-de-put-master-go-2026-08-16.zip`

Dentro: `cmd/`, `interno/`, `web/src`, `analise/`, `dados-base/`, `BRIEFING.md`, planilha, e as alterações que ainda não estavam commitadas em `servidor.go` / `App.tsx`. Fora do zip: `.git`, `.claude`, `node_modules`.

## `main-antes-do-merge-designs/`

Zip do checkout `C:\dev\venda-de-put` na `main` (`27cbe9d`) imediatamente antes do merge da branch `igorbeleza/designs` (2026-08-18).

Zip: `main-antes-do-merge-designs/venda-de-put-main-python-2026-08-18.zip`

Fora do zip: `.git`, venv, `__pycache__`, `.env`, snapshots, o zip da árvore Go acima.
